Los tests de integración HTTP con la app Express en memoria viven hoy en la raíz de `tests/` (`app.smoke.test.ts`, `auth.integration.test.ts`, etc.).
Convención futura: colocar aquí nuevos `*.integration.test.ts` y apuntarlos en `npm run test:integration`.
